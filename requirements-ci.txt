# CPU-only test dependencies for the low-resource CI job.
#
# The unit suite never loads a production TTS or Whisper model, so the heavy
# GPU packages in voice/requirements.txt (transformers, qwen-tts,
# openai-whisper, faster-whisper, silero-vad, accelerate) are deliberately
# excluded here -- tests that need them are skipped.
#
# This file exists so the CI job installs a *declared* dependency set instead of
# an inline list in the workflow. The previous workflow hand-copied four
# packages from voice/requirements.txt, so the two drifted with nothing to
# detect it.
#
# Everything below is imported at module scope by code the unit suite exercises.

# Audio analysis and mastering used by voice/mastering and voice/validator
pydub>=0.25.0
pyloudnorm>=0.1.1
librosa>=0.10.0
jiwer>=3.0.0
soundfile>=0.12.0
numpy>=1.26.0
scipy>=1.14.0

# Test runner. The suite is unittest-based, but a few modules use pytest
# fixtures and are collected only under pytest.
pytest>=8.0
