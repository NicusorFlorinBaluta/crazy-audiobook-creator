# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0
websockets>=13.0

# EPUB parsing
ebooklib>=0.18
beautifulsoup4>=4.12.0
lxml>=5.0

# LLM client
httpx>=0.27.0
json-repair>=0.47.0
# Optional at runtime unless external_validation.browser.enabled is true.
playwright>=1.54.0

# Data validation
pydantic>=2.9.0

# Database
aiosqlite>=0.20.0

# Configuration
pyyaml>=6.0
tzdata>=2025.2; platform_system == "Windows"
# Hard dependency: `from dotenv import load_dotenv` runs at import time in
# brain/dashboard/api/security.py, brain/dashboard/api/main.py and
# brain/orchestrator/nas_syncer.py. Without it the dashboard cannot start.
python-dotenv>=1.0.0

# NAS delivery sync over SFTP. Imported defensively in
# brain/orchestrator/nas_syncer.py, so the pipeline still runs without it -- the
# 24/7 NAS streaming feature is simply unavailable.
paramiko>=3.4.0

# Utilities
rich>=13.0
click>=8.1
numpy>=1.26.0
scipy>=1.14.0
soundfile>=0.12.0

# File upload handling
python-multipart>=0.0.9
