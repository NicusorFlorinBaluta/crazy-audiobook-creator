"""Route modules split out of the dashboard's main application.

Each module owns one cohesive slice of the API and is mounted by
`main.py`. Shared state and helpers live in `..runtime`, not here.
"""
