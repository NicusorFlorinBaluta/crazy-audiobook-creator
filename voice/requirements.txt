# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0
websockets>=13.0

# TTS engines (install the machine's GPU PyTorch build separately)
transformers==4.57.3
accelerate>=1.0.0
soundfile>=0.12.0
librosa>=0.10.0
# `qwen-tts` produces the actual audio, so an unpinned release can change voice
# cloning between two runs of the same book without any fingerprint change --
# invalidating reference-voice reuse and benchmark comparability.
#
# It is intentionally left unversioned HERE because the supported version is
# whatever the tested Windows/ROCm workstation resolved. Pin it in
# `constraints-windows-rocm-tested.txt` and install with that constraints file:
#
#   python -m pip install -r voice/requirements.txt \
#       -c voice/constraints-windows-rocm-tested.txt
#
# To read the currently installed version on the workstation:
#
#   python scripts/runtime_preflight.py
#
# `shared/runtime_preflight.py` warns when a quality-critical package is not
# constrained, so the gap is reported rather than silently tolerated.
qwen-tts

# Quality Validation
faster-whisper>=1.1.0
openai-whisper
silero-vad>=5.1.2
jiwer>=3.0.0

# Audio Processing
pydub>=0.25.0
pyloudnorm>=0.1.1
numpy>=1.26.0
scipy>=1.14.0

# Data & Config
pydantic>=2.9.0
pyyaml>=6.0
aiosqlite>=0.20.0

# Utilities
httpx>=0.27.0
requests>=2.32.0
rich>=13.0
plyer>=2.1.0
