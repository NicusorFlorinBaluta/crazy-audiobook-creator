# Direct runtime versions observed in the successful 2026-08-09 Windows AMD
# full-book run. Install the machine-specific PyTorch/ROCm build separately;
# it is intentionally not constrained to a public CUDA/CPU wheel here.
#
# Use this file when installing, so a reinstall cannot silently change the
# audio stack:
#
#   python -m pip install -r voice/requirements.txt \
#       -c voice/constraints-windows-rocm-tested.txt
#
# `shared/runtime_preflight.py` compares the installed versions of every
# audio-critical package against this file and reports anything unpinned or
# drifting. Keep it in sync when a release is validated.
transformers==4.57.3
faster-whisper==1.2.1
silero-vad==6.2.1
openai-whisper==20250625

# `qwen-tts` is the package that produces the audio. Pinned 2026-09-03 from the
# tested Windows/ROCm workstation venv (E:\PyTorch env\my_venv, Python 3.12.0,
# torch 2.9.1+rocmsdk20260116) via `python scripts/runtime_preflight.py`.
# To refresh after a validated release, re-run that script and take the
# reported `packages["qwen-tts"]` value.
qwen-tts==0.1.1
