# Scratch Utilities

These files are historical, project-specific debugging and repair scripts.
They are not part of the supported application or test suite.

Many refer to `sample_book`, mutate pipeline state, overwrite generated
artifacts, or assume an earlier schema. Read the entire script and back up the
target project before running anything in this directory.

Use the dashboard’s selection, reset, pause, and delete APIs for normal
operation.
